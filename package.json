{
  "name": "matrice-platform",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix analytics",
    "build": "npm run build --prefix analytics",
    "dev:standalone:support": "npm run dev --prefix support",
    "dev:standalone:support2": "npm run dev --prefix support2",
    "dev:standalone:training": "npm run dev --prefix training",
    "dev:standalone:marketplace": "npm run dev --prefix marketplace",
    "dev:standalone:annotation": "npm run dev --prefix annotation",
    "dev:analytics": "npm run dev --prefix analytics"
  }
}
